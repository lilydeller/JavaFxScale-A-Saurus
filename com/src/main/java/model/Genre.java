package model;

/**
 * the {@code Genre} enum represents the different musical genres
 * that a song can belong to in the application.
 */
public enum Genre {
    POP,
    CLASSICAL,
    JAZZ,
    ROCK;
}

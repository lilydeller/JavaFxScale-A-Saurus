package model;

/**
 * the {@code GamificationSystem} interface defines methods for updating
 * and displaying gamification progress and rewards within the application.
 */
public interface GamificationSystem {
    void updateProgress();
    void displayRewards();
}